# Completa la conexión PostgreSQL con tu contraseña local; no la subas a Git.
DATABASE_URL=
PORT=3000
HOST=127.0.0.1
CORS_ORIGINS=http://localhost:8081
# Las pruebas crean y eliminan su propio esquema aislado.
TEST_DATABASE_URL=
# URL accesible desde el dispositivo que abre el correo; incluye /api/v1.
PUBLIC_API_URL=http://localhost:3000/api/v1
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=
