# Esta aplicación no requiere claves de Google.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
# Activa el formulario solo cuando el correo de activación esté configurado.
EXPO_PUBLIC_REGISTRATION_ENABLED=false
# EAS activa esta opción en el APK de pruebas para mostrar solo la demo.
EXPO_PUBLIC_DEMO_ONLY=false
# Las búsquedas se abren directamente en Google Maps o Google.
